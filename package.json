{
  "name": "vaults.do",
  "version": "1.0.0",
  "description": "A Durable Object for managing API credentials",
  "module": "worker.js",
  "repository": "git@github.com:drivly/vaults.do.git",
  "author": "Samuel Lippert <samuel@driv.ly>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev": "wrangler dev worker.js --local true",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "devDependencies": {
    "jest": "^29.0.1",
    "prettier": "^2.7.1",
    "wrangler": "^2.1.4"
  }
}
